{
  "name": "any-agent-workspace",
  "private": true,
  "packageManager": "bun@1.2.22",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "bun run --filter any-agent build && packages/any-agent/bin/resume"
  }
}
